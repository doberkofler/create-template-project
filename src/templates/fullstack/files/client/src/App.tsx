import React from 'react';
import {Button, Container, Typography} from '@mui/material';

export const App = () => (
	<Container>
		<Typography variant="h2">Server Template - Client</Typography>
		<Button variant="contained">Hello from React/MUI</Button>
	</Container>
);
