# improvements

## more explicit use of package manager
currently the package.json template uses npm and the generator will later replace npm with pnpm or yearn and we should do this more explicitely using a variable like {{projectName}}.
